print("Hello, world!")

str name = "Pipa"

int i = 4

float f = 34.23

struct Token {
    str name
    int age
}

protocol Printable<T> {
    str toString(T)
}

protocol Iterator<T> {
    T next()
    bool hasNext()
}

bool compile(str filePath) {
    for entry in readDir(filePath) {
        print(entry)
    }
}

# string formatting

message = "Hello, {name + stuff}!"

Iterator<str> readlines(File file) {
    
}

file = openFile("program.pipa")
for line in readLines(file) {
    
}
    